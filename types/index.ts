// ⚠️ ARQUIVO ÓRFÃO — NÃO USAR.
//
// Este arquivo era um rascunho antigo de tipos, criado antes do schema real do
// banco existir. Nenhum arquivo do projeto o importa (todo o código usa
// `@/types`, que resolve para `types.ts` na raiz — esse sim é o arquivo vivo,
// com os tipos batendo com o schema real do Supabase).
//
// Ele foi esvaziado para não confundir ninguém que abrir esta pasta. O
// sandbox não consegue apagar fisicamente este arquivo (permissão do
// filesystem montado do Windows) — se quiser removê-lo de vez, apague a
// pasta `types/` pelo Explorer ou:
//   Remove-Item -Recurse -Force ".\types"
// (rodando no PowerShell, na raiz do projeto). Isso é opcional — o build não
// é afetado por este arquivo existir ou não.
export {}
